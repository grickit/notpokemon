// ----- CLASS: EntityOverlay {
  function EntityOverlay(args,map_override) {
    var self = this;
    Entity.call(this,args,map_override);

    // ----- Properties

    // ----- Methods

    // ----- Initialize
  }
// ----- }